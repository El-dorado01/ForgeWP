{
  "name": "forgewp",
  "private": true,
  "version": "0.0.0",
  "description": "Modern React-to-WordPress theme compiler",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/run-workspace.mjs dev",
    "build": "node scripts/run-workspace.mjs build",
    "typecheck": "node scripts/run-workspace.mjs typecheck",
    "preview": "node scripts/run-workspace.mjs preview",
    "export": "node scripts/run-workspace.mjs export",
    "fresh": "node scripts/run-workspace.mjs fresh",
    "sync:template": "node scripts/sync-cli-template.mjs",
    "sync:template-html": "node scripts/sync-cli-template.mjs",
    "sync": "node scripts/sync-to-wp.mjs",
    "export:sync": "node scripts/sync-to-wp.mjs",
    "install:template": "npm install --prefix packages/create-forgewp/template",
    "install:template-html": "npm install --prefix packages/create-forgewp/template-html",
    "test:create": "node packages/create-forgewp/index.js",
    "test:create-html": "node packages/create-forgewp/index.js --adapter html --yes --no-install",
    "test:compiler-adapters": "pnpm --filter @forgewp/compiler run test:adapter-runtime && pnpm --filter @forgewp/compiler run test:adapter-types"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.4.2"
  },
  "pnpm": {
    "overrides": {
      "vite": "$vite"
    }
  },
  "engines": {
    "node": ">=18"
  }
}
