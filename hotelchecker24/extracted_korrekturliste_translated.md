# hotelchecker24.com — QA Audit Correction List (English Translation)

**Complete crawl of all 35 URLs (DE + EN) · As of 2026-07-15 · Audit by maxonline.**

---

## Part 1: Correction List (Korrekturliste)

| No. | Priority | Category | Finding / Issue | Affected Pages (Scope) | Evidence / Proof | Concrete Fix | Responsible | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **1** | **Critical** | Function / Conversion | Inquiry button *"Jetzt Aufenthalt anfragen"* links to `/kontakt` — this URL returns a hard **404**. No visitor can submit a hotel inquiry (direct lead loss). | ALL 12 hotel pages (6 DE + 6 EN) + `/single-hotel` | `/kontakt` = HTTP 404; real page is at `/contact` (HTTP 200). In the JS bundle: `at("kontakt-page","/kontakt")` — Fallback points to incorrect, non-existent path. | Set the `kontakt-page` option in the Theme/WP to `/contact` OR change the hardcoded fallback from `/kontakt` to `/contact`. Additionally, create a 301 redirect from `/kontakt` → `/contact` as a safety net. | Developer (Theme/React) | |
| **2** | **Critical** | Function / URL | The generated inquiry link is constructed incorrectly: parameters contain unencoded spaces and umlauts in the server-rendered HTML (e.g., `inquiry_hotel=Kärntner Hof`) → technically invalid URL. | All hotel pages (SSR output) | `href="/kontakt?subject=hotel-inquiry&amp;inquiry_hotel=Kärntner Hof"` — raw space/umlaut. Client uses `encodeURIComponent`, SSR does not → SSR/CSR mismatch. | Encode parameters server-side as well using `encodeURIComponent` so that SSR and client outputs are identical and valid. | Developer (Theme/React) | |
| **3** | **Critical** | Function / Test | After fixing the path, verify that the contact form actually reads the `subject` and `inquiry_hotel` parameters and pre-fills the subject/hotel fields. Otherwise, the link will be reachable, but the hotel reference is lost. | `/contact` and `/en/contact-us` | Form is JS-rendered (Contact Form 7 + forgewp-hydrator). Parameter reading could not be verified externally. | Test End-to-End: click "Anfragen" (Inquire) on each hotel page → verify whether hotel name & subject arrive pre-filled in the form and the email is delivered. | Developer + QA | |
| **4** | **High** | SEO / Meta | Meta description is missing on all important non-detail pages. Google will then construct snippets uncontrollably — leading to poorer CTR on revenue-relevant pages. | Homepage, `/hotels`, `/hotelvergleiche`, `/contact`, `/about`, `/country/*`, `/impressum`, `/listicles` + all `/en` counterparts (~20 pages) | Grep across all pages: `name="description"` is only present on hotel detail and listicle pages, otherwise MISSING. | Add an individual, keyword-relevant meta description (120–160 characters) for each page (via the AIOSEO field per page). | SEO / Editors | |
| **5** | **High** | Navigation / Language | On German pages, both the top main menu AND the footer ("Navigation") are labeled in English ("Hotel Comparisons", "Hotels", "About Us") and link to `/en/` pages. In both places, German-speaking visitors are guided into the English section; the German internal linking is missing. | DE-Homepage + all DE-Pages — affects BOTH: header main menu AND footer navigation. | `home.html`: identical 3 EN links (`/en/hotel-comparisons`, `/en/hotels-2`, `/en/about-us`) in both the header and footer navigation blocks of the DE page. | Assign the German menu to the German area in WordPress — check Header and Footer separately (German labels, links to `/hotelvergleiche`, `/hotels`, `/about`). | WordPress Admin | |
| **6** | **High** | Indexing / Content | Two demo/placeholder pages from the theme are live, indexable, and in the sitemap: `/single-hotel` and `/single-listicle` with dummy content ("Single Hotel Vienna – Schubertring..."). `/single-hotel` also contains the broken contact link. | `/single-hotel`, `/single-listicle` | Both HTTP 200, no noindex, listed in `page-sitemap.xml`; text content = theme demo data. | Delete both pages (or set them to noindex) AND remove them from the sitemap. If needed as templates: maintain them as actual templates without a public URL. | WordPress Admin | |
| **7** | **High** | Structured Data | Double, competing JSON-LD blocks on EVERY page: Organization is present 2x (AIOSEO-@graph + separate 'ForgeWP Custom' snippet), BreadcrumbList also 2x. Redundant and potentially contradictory. | All 35 pages | JSON-LD type analysis: 2x Organization and 2x BreadcrumbList per page. | Consolidate to a single schema source (recommended: only AIOSEO). Remove the custom 'ForgeWP' Organization snippet and the duplicate breadcrumb. | Developer (Theme) | |
| **8** | **High** | Structured Data | Hotel schema with implausible rating: ALL 6 hotels carry an identical `aggregateRating` of 4.8 with a `ratingCount` of 1. An "aggregate" consisting of a single rating, identical everywhere, is critical for Google policy (Google may ignore the rich snippet or flag it as spam). | All 6 hotel detail pages (DE) + EN counterparts | Schema readout: each hotel has `ratingValue=4.8`, `ratingCount=1`. | Either provide real, individual rating numbers or remove `aggregateRating` completely. Do not use generic placeholder values. | Developer + Editors | |
| **9** | **High** | Structured Data | Hotel schema: the `addressCountry` field is empty (`""`) for all hotels — incomplete address markup. | All 6 hotel detail pages + EN | Schema readout: `addressCountry=''` for all hotels. | Correctly populate `addressCountry` ('AT' for Vienna/Austria, 'DE' for German hotels). | Developer (Theme) | |
| **10** | **Medium** | Internationalization | `hreflang` does not contain `x-default`. While the DE↔EN mapping is clean and reciprocal, the `x-default` hint for unspecified languages/regions is missing throughout. | 30 of 35 pages (all with hreflang) | hreflang check: `x-default` not found on any page. | Add the `x-default` alternate link (typically pointing to the DE or a language selector version). | Developer / SEO | |
| **11** | **Medium** | Internationalization | Two German listicles have no English counterparts and therefore have no `hreflang` markup/self-reference at all. | `/listicle/die-5-besten-boutique-hotels-in-wien-2026`, `/listicle/hotels-im-zentrum-von-wien-…` | hreflang check: `[MISSING]` on these two pages; EN sitemap contains only 1 listicle. | Create EN translations and link them OR intentionally set self-hreflang + x-default so that the markup is consistent. | Editors / SEO | |
| **12** | **Medium** | On-Page / Structure | Duplicate H1: On the listicle *"Die 10 besten Tagungshotels..."*, the same H1 heading appears twice (identical text). There should be exactly one H1 per page. | `/listicle/die-10-besten-tagungshotels-in-niederoesterreich-fuer-firmenseminare-2026` | H1 count = 2, identical text both times. | Demote the second H1 to H2 or fix the double output in the template. | Developer (Theme) | |
| **13** | **Medium** | Social / Open Graph | `og:image` is missing sitewide; `og:description` is missing on the homepage and list/archive pages. Sharing on social media or messengers yields empty, low-CTR previews. | Homepage, `/hotels`, hotel & listicle detail pages (`og:image` missing everywhere) | OG check: `og:image=N` on all checked pages; `og:description` missing on Home/Category. | Set an `og:image` for each page (hotel/featured image, min. 1200×630) and add `og:description` to index/archive pages. | Developer / SEO | |
| **14** | **Medium** | SEO / Meta | Duplicate title tags between DE and EN on index pages (e.g., 'Hotels - Hotelchecker24.com' is identical on `/hotels` and `/en/hotels-2`). EN titles are partially not localized. | `/hotels` vs `/en/hotels-2` | Title comparison: multiple identical `<title>` tags DE/EN. | Provide EN archive pages with unique, English titles; hotel names may remain the same. | SEO / Editors | |
| **15** | **Medium** | Structured Data / GEO | FAQ schema is inconsistent: 4 listicle pages have visible FAQ sections, but only 1 of them has the `FAQPage` schema. The schema is completely missing on 3 FAQ pages (including the English one). Note: FAQ rich results are barely visible on Google for commercial sites anymore — the value lies primarily in machine readability for AI search engines (GEO) and consistency. | OK: `/listicle/hotels-im-zentrum-von-wien-…` (FAQPage + 5 Questions). MISSING: `/listicle/die-5-besten-boutique-hotels-in-wien-2026`, `/listicle/die-10-besten-tagungshotels-…, /en/listicle/the-10-best-conference-hotels-…` | Schema readout: `FAQPage` block only on 1 out of 4 FAQ pages; the other 3 are without `FAQPage`/`Question`. | Serve `FAQPage` schema consistently on all pages with FAQ sections (matching question/answer text 1:1 with visible text). Ideally, generate this automatically from the FAQ block so it is not forgotten. | Developer + SEO | |
| **16** | **Low** | Structured Data | `Organization` schema has empty `sameAs` (no links to social/company profiles). | All pages (Organization block) | `sameAs: []` in custom Org snippet. | Add existing official profiles to `sameAs` — otherwise, remove it in the course of fixing Item 7. | Developer / SEO | |
| **17** | **Low** | Performance | `loading="lazy"` is inconsistently set (not at all on some pages). Above-the-fold images are correctly not lazy, but it is partially missing further down. | Listicle/Archive pages | Image check: hotel page 3/6 lazy, listicle 0/3 lazy. | Consistently deliver images below the fold with `loading="lazy"`; intentionally leave hero images eager. | Developer (Theme) | |
| **18** | **Low** | Slug Consistency | The German contact page uses the English slug `/contact` (instead of `/kontakt`). After fixing Item 1, the slug logic should be kept deliberately consistent. | `/contact` (DE) | page-sitemap: DE contact is at `/contact`, `/en/contact-us` for EN. | Decide: either change DE slug to `/kontakt` (with redirect) or intentionally keep `/contact` and align all links to it. | WordPress Admin | |

*Priority Legend: Critical = Broken function / immediate fix required · High = SEO/Indexing relevant · Medium = Optimization · Low = Housekeeping.*

---

## Part 2: Positively Verified — No Action Required (Geprüft OK)

| Area | Finding |
| :--- | :--- |
| **Redirects** | `http→https` and `www→non-www` are correctly routed via 301 redirects. |
| **Canonical** | All pages are correctly self-referencing; no incorrect references. |
| **hreflang Reciprocity** | DE↔EN mappings correctly point back to each other (only `x-default` is missing, see Item 10). |
| **EN Localization** | EN pages are fully translated, meta descriptions are in English, and `lang="en-US"` is correctly specified. |
| **Impressum (Legal Notice)** | Mandatory disclosures are complete (VAT ID `ATU82431815`, commercial register, media owner, phone, email). |
| **Internal Links** | Except for the `/kontakt` bug (Item 1), no broken internal links were found (all tested targets returned HTTP 200). |
| **Image Alt (SSR)** | Server-rendered images consistently have alt attributes. |
| **robots.txt & Sitemap** | Present, correctly referenced, and dynamically generated (AIOSEO). |
| **H1 (other pages)** | Except for the one listicle (Item 12), there is exactly one H1 per page. |
