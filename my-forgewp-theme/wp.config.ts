import type { ForgeWPThemeConfig } from "./src/lib/forgewp-config";

const config: ForgeWPThemeConfig = {
  name: "My Forgewp Theme",
  slug: "my-forgewp-theme",
  version: "0.1.0",
  description: "A ForgeWP theme: My Forgewp Theme",
  textDomain: "my-forgewp-theme",
};

export default config;
